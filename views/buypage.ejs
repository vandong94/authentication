<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Home Page</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

    <script>

        function checkBalance() {
            var balanceRemain = $("#balance").val();
            var selectBuy = $("#buySelect").val();
            //alert("remain: "+ balanceRemain);
            //alert("select: "+selectBuy);

            if (balanceRemain == 0 || selectBuy > balanceRemain) {
                document.getElementById("resultRemain").innerHTML = "Your balance remain is not enough to buy more!";
                return false;
            } else {
                return true;
            }
        }
    </script>
</head>
<body>
<div class="container">
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-6">
            <h2>Hi. Welcome To Buy Page</h2>

            <div>
                <div>Vui lòng chọn thẻ cào</div>
                <br/>
                <form method="post">
                    <select name="buySelect" id="buySelect">
                        <option value="10">Mệnh giá 10.000</option>
                        <option value="20">Mệnh giá 20.000</option>
                        <option value="30">Mệnh giá 30.000</option>
                    </select>
                    <br><br>
                    <input type="hidden" value="<%= data.balance %>" id="balance"/>
                    <p id="balanceRemain">Your balance remain: <%= data.balance %></p>
                    <p id="resultRemain" style="color:red"></p>

                    <input type="submit" onclick="return checkBalance()" value="Buy Now"/>

                </form>
            </div>
            <br>
            <a href="/">Trang Chủ</a>
            <a href="/auth/logout">Sign Out</a>

        </div>
        <div class="col-md-3"></div>
    </div>
    <div class="row" style="text-align: center">
    </div>
</div>

</body>
</html>